# model/

YOLOv8 weights are **downloaded automatically** on first server start.

The file `yolov8n.pt` (~6 MB) will appear here after the first request
reaches the `/analyze-image` endpoint.

Do **not** commit binary weight files to version control.
Add `*.pt` and `*.onnx` to your `.gitignore`.
